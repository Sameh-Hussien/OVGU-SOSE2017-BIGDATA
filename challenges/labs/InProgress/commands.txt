$ sudo groupadd barca

$ sudo groupadd merengues

$ sudo useradd -u 2010 neymar

$ sudo useradd -u 2016 ronaldo

$ sudo usermod -g barca ronaldo

$ sudo usermod -g merengues neymar

$ grep 'ronaldo\|neymar' /etc/passwd

```
neymar:x:2010:502::/home/neymar:/bin/bash
ronaldo:x:2016:501::/home/ronaldo:/bin/bash
```

$ grep 'merengues\|barca' /etc/group

```
barca:x:501:
merengues:x:502:
```
$ wget http://download.softagency.net/MySQL/Downloads/Connector-J/mysql-connector-java-5.1.42.tar.gz
$ tar zxvf mysql-connector-java-5.1.42.tar.gz
$ sudo mkdir /usr/share/java
$ sudo cp mysql-connector-java-5.1.42/mysql-connector-java-5.1.42-bin.jar /usr/share/java/mysql-connector-java.jar


** client mysql
$ sudo yum install mysql

** start mysql
$ sudo service mysql start


$ sudo curl https://archive.cloudera.com/cm5/redhat/6/x86_64/cm/cloudera-manager.repo > cloudera-manager.repo


$ sudo /usr/share/cmf/schema/scm_prepare_database.sh mysql -uroot -pcloudera cmdb cmdb cloudera